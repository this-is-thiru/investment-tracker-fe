import { Routes } from '@angular/router';
import { TaxFilingComponent } from './pages/tax-filing/tax-filing.component';

export const TAX_FILING_ROUTES: Routes = [
  { path: '', component: TaxFilingComponent },
];
